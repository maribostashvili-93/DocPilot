<!-- BATCAVE:START -->
This repo is connected to Batcave. For product, planning, implementation, release, or project-management work, use Batcave as the source of truth.

1. Run `batcave project status` from the repo root.
2. Read `batcave project context --format markdown` before changing project scope or implementation state.
3. Write task status, comments, evidence, and PR links back to Batcave.
4. If Batcave is unavailable, mark local work as pending reconciliation instead of claiming Batcave state.
<!-- BATCAVE:END -->
